# 完成说明

## 我做了什么

我实现了一个与 ROS 2 和硬件解耦的机器人迎宾状态机。它支持首次迎宾、同一在场周期去重、对话/会议期间抑制效果、离场 10 秒后由 `TICK` 触发送客、短暂离开返回，以及完整离场后开启新接待周期。

状态按人员分别保存，因此多人不会互相覆盖；对话与会议是应用级状态。所有外部输入使用不可变的 `Event`，输出使用不可变的 `Effect`。`snapshot()` 返回深拷贝，外部修改不会污染内部状态。

## 我如何验证

我用 `pytest` 编写了 11 个测试，覆盖题目要求的六类场景，并额外检查了多人独立状态、非法事件、时间倒序和非法超时配置。执行 `python -m pytest -q` 的结果为 `11 passed`，另外执行了 Python 编译检查和 Git whitespace 检查。

## 文档

- `PLAN.md`：实现计划和风险判断。
- `ARCHITECTURE.md`：模块依赖、职责、状态归属和未来扩展方式。
- `REPORT.md`：完成情况、测试结果及 ROS 2 故障判断。
- `AI_USAGE.md`：AI 的用途及验证方式。

## 关键判断

ROS 2 日志中的 `accepted_async` 只表示桥接层接受了请求，不代表机器人完成挥手。现有证据显示 action server 数量为 0 且 `robot-action.service` 未运行，因此当前不应直接执行真实动作，应先恢复并验证动作服务与 action server。
