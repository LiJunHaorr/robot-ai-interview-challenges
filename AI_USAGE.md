# AI Usage

AI was used to help structure the state machine, draft the initial implementation, tests, architecture diagram, and ROS 2 diagnostic report. I verified the result by reading the challenge contract, reviewing the state transitions, checking that the public import paths match the requested API, and running the complete pytest suite. The implementation deliberately keeps all hardware integration outside the business module.
